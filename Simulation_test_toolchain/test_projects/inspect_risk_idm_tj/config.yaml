dataset:
  name: sind
  location: tj
  data_dir: datasets/SinD_dataset
  desired_dt: 0.1
  use_lanelet2_maps: true

scenario:
  scene_index: 1
  init_timestep: 10
  num_steps: 150
  ego_selection_strategy: longest_trajectory
  ego_agent_name:

simulation:
  mode: ego_closed_loop
  history_sec: 2.0
  future_sec: 4.0
  neighbor_radius: 50.0

policies:
  ego_policy: risk_idm
  non_ego_policy: ground_truth
  ego:
    desired_velocity: 8.0
    max_acceleration: 3.0
    min_acceleration: -5.0
  non_ego: {}

checkpoints:
  asaprl_ckpt_path: Simulation_test_toolchain/checkpoints/ckpt_best_iter497000_step417741440.pth.tar
  diffuser_ckpt_path: Simulation_test_toolchain/checkpoints/v2.0.ckpt

output:
  project_name: inspect_risk_idm_tj
  root_dir: Simulation_test_toolchain/test_projects
  save_html: true
  save_json: true
  save_csv: true

visualization:
  enabled: true
  map_margin_m: 15.0
  width: 1200
  height: 760
